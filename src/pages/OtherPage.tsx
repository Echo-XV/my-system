import { motion } from 'framer-motion';

export default function OtherPage() {
  return (
    <motion.div layout layoutId='other-page' className='w-screen h-screen'>
      <h1>Other Page</h1>
    </motion.div>
  );
}